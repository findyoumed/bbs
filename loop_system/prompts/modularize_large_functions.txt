[목표]
아래 4개의 거대 함수(>250줄)를 안전하게 모듈화합니다.
한 사이클에 딱 1개 함수에서 작은 하위 함수 1~2개만 추출합니다.

[대상 함수 목록]
1. public/js/app.js → showSignup() (912줄, line 2538~)
2. public/js/app.js → handleCmd() (253줄, line 3732~)
3. public/js/core/postScreens.js → createPostScreens() (260줄, line 1~)
4. scripts/smoke-ui-geometry.js → main() (320줄, line 290~)

[우선순위]
- 가장 독립적이고 안전한 것부터 추출
- showSignup() 내부의 validation/UI helper 함수 먼저 추천
- handleCmd()는 이미 commandRouter*.js로 분리 진행 중이므로 나중에
- scripts/ 파일은 테스트 코드이므로 우선순위 최하위

[규칙]
- 한 사이클에 1개 함수에서 의미있는 하위 함수 1~2개만 추출
- 부팅 경로, 라우팅, 인증 흐름은 절대 건드리지 않음
- 추출한 함수는 같은 디렉토리의 새 파일로 분리
  예: showSignup() 내부 함수 → public/js/core/signupXxx.js
- import/export 이름은 단순하고 명확하게 유지
- 기존 동작 100% 보존

[절대 금지]
- 한꺼번에 여러 함수 분리
- app.js 전체를 import 구조로 갈아엎기
- showSignup(), handleCmd() 자체를 통째로 이동
- 동작 확인 없이 추가 리팩토링
- 요청 범위 밖 변경

[검증 명령]
npm run smoke:vercel-ready
npm run smoke:boards
