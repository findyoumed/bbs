# AI 코딩 에이전트 규칙 (BBS 프로젝트)

> Antigravity, Windsurf, Cursor, Cline, Claude Code, Gemini Code, ChatGPT Codex 등 모든 AI 도구 적용. 이 파일은 프로젝트의 **헌법**이며 모든 에이전트는 이를 최우선으로 준수해야 합니다.

---

## 1. 5대 절대 규칙 (Mandatory Rules)

| #   | 규칙 (Rule)             | 상세 내용 및 이유                                                                                                                                     |
| --- | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **코드 생략 절대 금지** | 수정 사항이 적더라도 **전체 코드를 생략 없이 출력**할 것. `// ...` 또는 `/* rest of code */` 사용 금지. 초보 사용자는 생략된 부분을 채울 수 없습니다. |
| 2   | **추측 금지**           | 불확실하면 즉시 멈추고 **질문**하거나, 환경/설정을 먼저 읽으세요. AI의 추측은 버그의 근원입니다.                                                      |
| 3   | **요청받은 것만 수행**  | 요청 외 리팩토링, 스타일 변경, 추가 기능 제안 금지. 기존의 잘 작동하는 로직을 건드려 새 버그를 만드는 것을 방지합니다.                                |
| 4   | **기존 코드 보존**      | 파일을 덮어쓰기 전 반드시 기존 코드를 읽고 분석하세요. 중요한 주석이나 로직을 무단 삭제하지 마세요.                                                   |
| 5   | **WORK_LOG 기록**       | 모든 작업(문서 수정 포함)은 `WORK_LOG.md`에 기록하고, 코드에는 `LOG_ID` 주석을 남깁니다.                                                              |

---

## 2. 작업 프로세스 및 정책

### 2.1 매 작업 전 4단계

1. **읽기 (Read)**: `WORK_LOG.md`와 수정 대상 파일, 관련 규칙을 먼저 읽습니다.
2. **생각 (Think)**: 요구사항, 기존 구조, 변경 범위를 정리합니다. (즉시 코드 생성 금지)
3. **계획 (Plan)**: 3~5단계로 절차를 나누고, 변경 파일과 범위를 명시하여 사용자 확인을 받습니다.
4. **실행 및 검증 (Execute & Verify)**: 코드를 작성하고 반드시 검증 명령을 실행하여 결과를 확인합니다.

### 2.2 모듈화 및 로직 보호 정책

- **모듈화 원칙**: 한 파일이 너무 길어지거나 함수가 한 가지 이상의 일을 하면 분리를 제안하되, **자동으로 분리하지 말고** 먼저 보고 후 승인을 받으세요. (함수 250줄 초과 시 주의)
- **비즈니스 로직 보호**: 인증, 상태 관리, 데이터 처리 등 핵심 로직 수정 시 **삭제/변경 이유를 명확히 설명**해야 합니다.
- **변경 투명성**: 작업 후 무엇이 바뀌었는지 명확히 설명하세요. 동작 변화가 없다면 그 사실도 명시합니다.

### 2.3 권한 정책

- 이 저장소에서는 `Bash`, `Edit`, `Write` 명령을 **가능한 한 승인 질문 없이** 수행할 수 있습니다. (YOLO 모드 지향)
- 단, 새 npm 패키지 추가는 예외 없이 승인이 필요합니다.

---

## 3. 프로젝트 아키텍처 (필독)

### 3.1 기술 스택

- **Core**: Vanilla JS (ES6+), HTML5, CSS3, Node.js 내장 모듈
- **Storage/Auth**: Supabase JS SDK
- **금지 기술**: React, Vue, TypeScript, Webpack, Vite, Next.js 등 프레임워크/컴파일러 금지

### 3.2 파일 구조

- `public/js/app.js`: 브라우저 단일 엔트리 (ES modules) → `initApp()` in `core/appFactory.js`
- `public/js/core/*.js`: 팩토리 함수 패턴 기반의 모듈화된 브라우저 로직 (상태/입력/연동)
- `public/js/core/lazyModuleFactory.js`: 지연 로딩 패턴으로 선택적 기능을 초기 의존성 외로 빼기
- `src/server/*`: Node.js 서버 로직 (API 핸들러, 서비스, 레포지토리)
- `src/core/`: 서버/도구 공유 유틸 (`AssetManager.js`, `TemplateEngine.js`)
- `legacy/`: 레거시 BBS 데이터 (`.mnu`, `.txt`)

### 3.3 브라우저 상태 관리 (`state.screen`) 및 라우팅

모든 화면 전환은 `state.screen` 값에 따라 결정되며 URL과 동기화됩니다.

| state.screen     | Clean URL           | 설명             |
| ---------------- | ------------------- | ---------------- |
| `'main'`         | `/`                 | 메인 대문        |
| `'board-select'` | `/menu/{path}`      | 게시판 목록/메뉴 |
| `'post-list'`    | `/board/{id}`       | 게시물 목록      |
| `'post-view'`    | `/board/{id}/{pId}` | 게시물 본문      |
| `'chat-lobby'`   | `/chat`             | 대화실 로비      |
| `'signup'`       | `/signup`           | 회원가입 플로우  |

### 3.4 터미널 UI 구조

```text
#terminal-wrapper (80x24 셀, 8px*16px)
  ├── #terminal-container (ANSI 텍스트 렌더링)
  └── #terminal-overlay (입력창/커서/인터랙티브 레이어)
```

---

## 4. WORK_LOG 및 검증

### 4.1 WORK_LOG.md 형식

```markdown
## [YYYY-MM-DD HH:MM] 작업 제목

**LOG_ID: YYYYMMDD_HHMM**
목표: ...
변경 파일: public/js/core/Example.js
수행 작업: 1) ... 2) ...
실행: `npm run smoke:vercel-ready`
기대: ...
결과: ✅ 완료
```

### 4.2 주요 검증 명령

- `npm run dev`: 로컬 서버 실행 (기본 3002포트, PORT 환경변수로 변경 가능)
- `npm run smoke:vercel-ready`: 배포 전 전체 검증
- `npm run loop:verify`: 완료 게이트 — 모든 검증(9개 smoke 스크립트 + qa:final) 순차 실행
- `npm test`: 유닛 테스트 실행
- `node --check [파일명]`: JS 문법 체크 (수정 후 필수)

---

## 5. Git 규칙

- **커밋**: 의미 있는 진전마다 로컬 커밋 수행.
- **보험 커밋**: 큰 작업 전 `chore: before AI refactor` 커밋 권장.
- **부분 추가**: `git add .` 대신 `git add -p`를 사용하여 의도치 않은 변경 포함 방지.

---

## 6. PC통신 01410 핵심 레트로 가이드라인 및 참조 자료

- **원전 자료 참조**: `docs/오늘 추억속으로 사라진 01410의 마지막 모습 - 푸른하늘 임묵 네이버 카페.mp4` (1990~2000년대 대한민국 PC통신 전용 접속번호 `01410` 고화질 역사 영상)
- **북마크 아카이브**: [docs/PC통신_참고_북마크_리스트.md](file:///d:/work/bbs/www-bbs/docs/PC통신_참고_북마크_리스트.md) (01410 에뮬레이션, ezbbs, DOS 게임, 아카이브 오픈소스 및 위키 자료집)
- **UI/UX 레트로 표준**:
  - **헤더 타이틀**: `PC통신동호회 01410` 브랜드 정체성 유지.
  - **80x24 ANSI 터미널**: 모뎀 시대를 계승하는 텍스트 터미널 라인 간격 및 1-cell 커서 정렬 준수.
  - **프롬프트 고정 규칙**: 화면 마운트 시 프롬프트(`선택 >>`, `입력 >>` 등)가 사용자 입력 실수 시 하단 푸터로 쫓겨나지 않도록 본문 인라인 호스트 자리를 철통 사수.

